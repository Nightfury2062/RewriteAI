# RewriteAI — Frontend

This directory contains the React + Vite frontend for RewriteAI.

> For full project documentation, setup instructions, and API reference, see the [root README](../README.md).

## Development

```bash
npm install
npm run dev
```

Runs on `http://localhost:5173`. Requires the backend to be running on `http://localhost:5000`.
